interface:
  display_name: "README Authoring"
  short_description: "Write READMEs around a reader's first useful result"
  default_prompt: "Use $readme-authoring to explain the reader's problem and project value, show a concrete representative result, and verify the first-use path from repository evidence."
